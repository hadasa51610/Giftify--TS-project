{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "serve": "http-server -c-1 .",
    "start": "npm run build && npm run serve"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
